x <- c(12, 7, 8, 9, 5, 15, 1, -8, -9)

# Média
result.mean <- mean(x)
print(result.mean)

# Mediana
result.median <- median(x)
print(result.median)

# Moda
result.mode <- mode(x)
print(result.mode)

# Desvio Padrão
result.range <- range(x)
print(result.range)

# Variância
result.var <- var(x)
print(result.var)

# Desvio Padrão
result.sd <- sd(x)
print(result.sd)

# Coeficiente de Variação
result.cv <- sd(x) / mean(x)
print(result.cv)
